export type Product = { //could be Interface also
    id: number
    name: string
    description: string
    price: number
    pictureUrl: string,
    type: string,
    brand: string,
    quantityInStock: number 
}